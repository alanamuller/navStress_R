# Randomize list of learning trials
# Probably don't have double ones together though

# make list when outer is more familiar
list <- c("O", "O", "O", "O", "I", "I")

# randomize list
rando_list <- sample(list)

print(rando_list)



# make list when inner is more familiar
list <- c("I", "I", "I", "I", "O", "O")

# randomize list
rando_list <- sample(list)

print(rando_list)

